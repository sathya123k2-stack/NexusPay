apiVersion: v1
kind: Service
metadata:
  name: nexuspay-transactionservice
spec:
  type: NodePort
  selector:
    app: nexuspay-transactionservice
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30082