apiVersion: v1
kind: Service
metadata:
  name: nexuspay-accountservice
spec:
  type: NodePort
  selector:
    app: nexuspay-accountservice
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081
