apiVersion: v1
kind: Service
metadata:
  name: nexuspay-customerservice
spec:
  type: NodePort
  selector:
    app: nexuspay-customerservice
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080