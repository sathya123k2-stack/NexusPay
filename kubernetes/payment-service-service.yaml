apiVersion: v1
kind: Service
metadata:
  name: nexuspay-paymentservice
spec:
  type: NodePort
  selector:
    app: nexuspay-paymentservice
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30083