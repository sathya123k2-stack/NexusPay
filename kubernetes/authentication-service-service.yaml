apiVersion: v1
kind: Service
metadata:
  name: nexuspay-authenticationservice
spec:
  type: NodePort
  selector:
    app: nexuspay-authenticationservice
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30084